# CONTEXT

This project tracks the architectural reorganization of the `niri_state` package into subdirectories (`api`, `core`, `adapters`, `observability`) using a compatibility-first approach with temporary re-export shims.

Primary planning document:
- `ORGANIZATION_REFACTOR.md`
