# ASSUMPTIONS

1. The attached `.context/niri-pypc` codebase is canonical dependency truth for this planning pass.
2. Existing brainstorming docs are useful but may contain drift; final concept must resolve drift toward dependency reality.
3. `niri-state` remains a downstream library and must not reimplement transport/protocol responsibilities.
4. The output requested now is a comprehensive final `CONCEPT.md` (not implementation code).
