# Assumptions

- Scope is this repository only (`niri-state`), not direct edits to `niri-pypc`.
- "Implement all fixes" means all concrete fix items in the report that are applicable here and can be validated by tests.
- Existing architecture (immutable snapshots + reducer loop) remains, unless report explicitly calls for correctness fixes.
